'use client';

import React from 'react';
import styled from 'styled-components';

const StyledContacts = styled('h1')`
  color: red;
`;

function Contacts() {
  return <StyledContacts>Hello, Contacts!</StyledContacts>;
}

export default Contacts;
