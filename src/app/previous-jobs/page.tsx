'use client';

import React from 'react';
import styled from 'styled-components';

const StyledPreviousJobs = styled('h1')`
  color: red;
`;

function PreviousJobs() {
  return <StyledPreviousJobs>Hello, PreviousJobs!</StyledPreviousJobs>;
}

export default PreviousJobs;
