'use client';

import React from 'react';
import styled from 'styled-components';

const StyledPortfolio = styled('h1')`
  color: red;
`;

function Portfolio() {
  return <StyledPortfolio>Hello, Portfolio!</StyledPortfolio>;
}

export default Portfolio;
